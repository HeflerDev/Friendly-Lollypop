/*
const foesModule = require('../foesModule');

describe('when creating a bat foe', () => {
  
  const foe = foesModule.Foe('bat', {}).bat;
  
  test('hello', () => {
      
    expect(foe.animations.loadSprites()).toBeDefined();

  })

});
*/
